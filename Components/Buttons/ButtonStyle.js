import {StyleSheet} from 'react-native'

const ButtonStyle = StyleSheet.create({
    wrapper: {
        marginTop: 20,
        alignItems: 'center'
    },

    buttonContainer: {
        width: 260,
        alignItems: 'center',
        backgroundColor: '#47AE4f',
        borderRadius: 15
    },

    buttonText: {
        padding: 20,
        color: 'white',
    }
})

export default ButtonStyle